% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_llm.R
\name{llm_solver}
\alias{llm_solver}
\title{Solve vector of prompts with optional schema, cache, retries, and repetitions}
\usage{
llm_solver(
  prompts,
  chat,
  schema = NULL,
  cache_dir = NULL,
  reps = 1L,
  max_attempts = 20L,
  max_active = 100L,
  rpm = 1000,
  cache_batch_size = 50L
)
}
\arguments{
\item{prompts}{Character vector or list of prompt texts. Each input prompt
becomes one logical row before \code{reps} expansion.}

\item{chat}{Ellmer chat object used as a template. The provider settings,
model, extra arguments, and system prompt are read from this object, and a
clone is created for each cache chunk so chunk solves do not share mutable
chat state.}

\item{schema}{Optional ellmer schema. When \code{NULL}, the solver runs in text
mode and returns a \code{text} column. When supplied, the solver runs in
structured mode and returns one column per schema field. The schema is also
hashed into the cache key, so changing it invalidates old cached results.}

\item{cache_dir}{Optional directory for \code{result_journal.jsonl} and
\code{result_index.rds}. When a path is supplied, completed rows are cached to
disk and reused across reruns. When \code{NULL}, disk caching is disabled, but
identical prompt rows are still deduplicated within the current call via an
in-memory index.}

\item{reps}{Positive integer number of intentional repeated solves per
prompt. Each prompt is expanded to \code{rep = 1:reps}, and \code{rep} is included in
the cache key, so repeated solves do not collapse onto the same cached row.}

\item{max_attempts}{Positive integer retry budget applied only to unresolved
rows. Rows that still fail after the final attempt are returned with \code{NA}
payload fields plus \code{has_error = TRUE} and \code{error_message}.}

\item{max_active}{Optional positive integer cap on concurrently active
requests within one cache chunk. Lower values reduce pressure on the
provider; higher values increase throughput if the provider tolerates it.
The default is \code{100L}. When \code{NULL}, the solver removes the concurrency cap
and allows up to all rows in the current chunk to run, so request pacing is
controlled only by \code{rpm}. The effective value per chunk is always capped at
the number of rows in that chunk.}

\item{rpm}{Optional requests-per-minute budget for the underlying ellmer
parallel call. Lower values make the solver more conservative; higher
values let it issue requests faster, subject to any stricter provider-side
rate limits. When \code{NULL}, the wrapper omits explicit RPM throttling and
relies on \code{max_active} alone. \code{rpm} and \code{max_active} cannot both be
\code{NULL}.}

\item{cache_batch_size}{Positive integer number of unique unresolved cache
keys to solve before forcing a cache flush. Smaller values improve crash
resilience by persisting results more often; larger values reduce overhead
but increase the amount of completed work that can remain unflushed inside
the currently running chunk.}
}
\value{
Tibble with one row per prompt-repetition. Always includes
\code{row_id}, \code{rep}, \code{cache_key}, \code{from_cache}, \code{attempts_used}, \code{has_error},
and \code{error_message}. Text mode adds \code{text}; structured mode adds one
column per schema field.
}
\description{
Wraps native \code{ellmer} parallel chat helpers with an outer loop that caches
completed rows on disk, retries only unresolved rows, and supports repeated
solves of the same prompt via \code{reps}.
}
